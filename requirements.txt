requests==2.31.0       # For sending HTTP requests to the BKK GTFS-RT API
protobuf               # For decoding the GTFS-RT binary protocol buffers
pandas==2.1.1          # For parsing and processing static GTFS data (e.g., stops.txt, routes.txt)
tabulate==0.9.0        # For creating BKK-style terminal tables for tram timetables
geopy==2.4.0           # For calculating distances between stops and tram positions (optional)
